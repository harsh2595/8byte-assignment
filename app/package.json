{
  "name": "8byte-sample-platform-app",
  "version": "1.0.0",
  "description": "Small API used to demonstrate DevOps assignment infrastructure, CI/CD, observability, and database connectivity.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "lint": "node --check src/*.js test/*.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "prom-client": "^15.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
